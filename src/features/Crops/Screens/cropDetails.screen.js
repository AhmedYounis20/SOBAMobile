import { View } from "react-native";

export const CropDetailsScreen = () => {
  return <View></View>;
};

/*
 * plant scene and its name
 * life form (seeding)
 * has a diesise?
 * expected harvest time
 * expected yield
 * time-series chart of health of crop
 */
